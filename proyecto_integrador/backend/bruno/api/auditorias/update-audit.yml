info:
  name: Update Audit
  type: http
  seq: 6

http:
  method: PATCH
  url: '{{baseUrl}}/api/auditorias/1'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "estado_reportado_id": 2,
        "comentarios": "Auditoría actualizada"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
