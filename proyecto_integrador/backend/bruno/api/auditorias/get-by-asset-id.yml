info:
  name: Get Audits by Asset ID
  type: http
  seq: 4

http:
  method: GET
  url: '{{baseUrl}}/api/auditorias/activo/1'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
