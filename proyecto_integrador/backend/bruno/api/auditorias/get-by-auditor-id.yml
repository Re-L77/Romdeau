info:
  name: Get Audits by Auditor ID
  type: http
  seq: 5

http:
  method: GET
  url: '{{baseUrl}}/api/auditorias/auditor/1'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
