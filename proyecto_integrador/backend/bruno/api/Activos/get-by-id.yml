info:
  name: Get Asset by ID
  type: http
  seq: 4

http:
  method: GET
  url: '{{baseUrl}}/api/activos/1'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
