info:
  name: Create User
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/api/usuarios
  body:
    type: json
    data: |-
      {
        "email": "proxy@romdeau.com",
        "nombres": "XD ",
        "apellido_paterno": "pe",
        "apellido_materno": "ne",
        "rol_id": 2
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
