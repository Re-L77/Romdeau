info:
  name: Cost by Department
  type: http
  seq: 3

http:
  method: GET
  url: '{{baseUrl}}/api/analytics/cost-by-department'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
