info:
  name: User Performance
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/api/analytics/user-performance'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
