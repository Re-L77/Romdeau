info:
  name: Get all assets
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/activos"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
