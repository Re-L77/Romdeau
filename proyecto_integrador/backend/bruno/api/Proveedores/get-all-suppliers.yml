info:
  name: Get All Suppliers
  type: http
  seq: 1

http:
  method: GET
  url: '{{baseUrl}}/api/proveedores'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
