info:
  name: Get Supplier by ID
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/api/proveedores/1'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
