info:
  name: Listar no leidas
  type: http
  seq: 3

http:
  method: GET
  url: '{{baseUrl}}/api/notificaciones?no_leidas=true'
  auth:
    type: bearer
    token: '{{token}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
