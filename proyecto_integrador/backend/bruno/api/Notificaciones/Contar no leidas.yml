info:
  name: Contar no leidas
  type: http
  seq: 4

http:
  method: GET
  url: '{{baseUrl}}/api/notificaciones/no-leidas/count'
  auth:
    type: bearer
    token: '{{token}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
