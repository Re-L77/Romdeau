info:
  name: Marcar todas leidas
  type: http
  seq: 6

http:
  method: PATCH
  url: '{{baseUrl}}/api/notificaciones/leer-todas'
  auth:
    type: bearer
    token: '{{token}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
