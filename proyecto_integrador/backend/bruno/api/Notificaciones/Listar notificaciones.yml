info:
  name: Listar notificaciones
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/api/notificaciones'
  auth:
    type: bearer
    token: '{{token}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
