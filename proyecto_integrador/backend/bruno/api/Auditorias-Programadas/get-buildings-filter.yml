info:
  name: Get Buildings Filter
  type: http
  seq: 11

http:
  method: GET
  url: '{{baseUrl}}/api/auditorias-programadas/filtros/edificios'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
