info:
  name: Get Form Catalogs
  type: http
  seq: 4

http:
  method: GET
  url: '{{baseUrl}}/api/auditorias-programadas/filtros/formulario'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
