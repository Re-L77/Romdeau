info:
  name: Update Scheduled Audit Status
  type: http
  seq: 7

http:
  method: PATCH
  url: '{{baseUrl}}/api/auditorias-programadas/1/estado/2'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
