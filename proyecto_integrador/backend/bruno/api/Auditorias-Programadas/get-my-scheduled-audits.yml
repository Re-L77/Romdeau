info:
  name: Get My Scheduled Audits
  type: http
  seq: 3

http:
  method: GET
  url: '{{baseUrl}}/api/auditorias-programadas/mias'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
