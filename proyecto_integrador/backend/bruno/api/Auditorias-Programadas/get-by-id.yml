info:
  name: Get Scheduled Audit by ID
  type: http
  seq: 5

http:
  method: GET
  url: '{{baseUrl}}/api/auditorias-programadas/1'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
