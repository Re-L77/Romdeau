info:
  name: Update Scheduled Audit
  type: http
  seq: 6

http:
  method: PATCH
  url: '{{baseUrl}}/api/auditorias-programadas/1'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "titulo": "Auditoría Actualizada",
        "descripcion": "Nueva descripción",
        "fecha_programada": "2026-04-20T10:00:00Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
