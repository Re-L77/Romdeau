info:
  name: Get Audits by Status
  type: http
  seq: 9

http:
  method: GET
  url: '{{baseUrl}}/api/auditorias-programadas/por-estado/1'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
