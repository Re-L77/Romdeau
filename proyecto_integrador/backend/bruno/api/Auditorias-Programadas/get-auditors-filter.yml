info:
  name: Get Auditors Filter
  type: http
  seq: 10

http:
  method: GET
  url: '{{baseUrl}}/api/auditorias-programadas/filtros/auditores'
  headers:
    - name: Authorization
      value: 'Bearer {{token}}'
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
