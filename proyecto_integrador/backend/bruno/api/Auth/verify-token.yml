info:
  name: Verify Token
  type: http
  seq: 6

http:
  method: POST
  url: '{{baseUrl}}/api/auth/verify-token'
  auth:
    type: bearer
    token: '{{token}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
