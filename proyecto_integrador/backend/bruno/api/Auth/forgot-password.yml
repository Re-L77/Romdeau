info:
  name: Forgot Password
  type: http
  seq: 4

http:
  method: POST
  url: '{{baseUrl}}/api/auth/forgot-password'
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "124051537@upq.edu.mx"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
